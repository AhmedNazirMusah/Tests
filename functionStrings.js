const strLength = (str) => {
    return str.length;
}

const reverseString = (str) => {
    reverse = str.split('').reverse().join('')
    return reverse;
}

module.exports = {strLength,reverseString}